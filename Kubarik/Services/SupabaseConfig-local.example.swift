//
//  SupabaseConfig-local.example.swift
//  Kubarik
//
//  Template for `SupabaseConfig-local.swift`. To use:
//
//    1. Duplicate this file in Xcode → rename to `SupabaseConfig-local.swift`
//    2. Replace placeholder values with your Supabase project URL + anon key
//    3. Add the new file to the Kubarik build target
//
//  Notes:
//    • Files matching `*-local.swift` are gitignored, so your real keys stay local.
//    • The anon key is client-facing by design — RLS enforces access control.
//    • Never ship the service_role key inside the app binary.
//

/*
import Foundation

enum SupabaseConfigLocal {
    static let projectURLString = "https://YOUR-PROJECT.supabase.co"
    static let anonKey = "YOUR_SUPABASE_ANON_KEY"
}
*/
