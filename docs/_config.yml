title: Kubariki
description: Cozy block-puzzle game for iOS
baseurl: "/Kubarik"
permalink: pretty

# No theme — we ship our own HTML/CSS via the default layout.

# Tell Jekyll to keep the favicon SVG and other asset paths intact
include:
  - assets

defaults:
  - scope:
      path: ""
    values:
      layout: default
